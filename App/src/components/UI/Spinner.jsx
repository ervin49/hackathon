import React from 'react';

const Spinner = () => {
  return (
    // Utilizează o animație simplă și stilizare Tailwind CSS
    <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-500"></div>
  );
};

export default Spinner;